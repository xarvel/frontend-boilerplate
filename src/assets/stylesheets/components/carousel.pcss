.carousel {
  overflow: hidden;

  touch-action: none;
  user-select: none;
  -webkit-user-drag: none;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);

  &__container {
    display: flex;
    will-change: transform;
  }
}
.carousel-wrapper{
  display: flex;
}